# Vulkan Renderer

A minimal, high-performance rendering engine built with Vulkan.
Focused on explicit control, low overhead, and modern GPU techniques.

## Build

```bash
git clone https://github.com/sajvn0x/vulkan-renderer.git --recursive
make
```
